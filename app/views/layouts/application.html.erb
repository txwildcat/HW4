<!DOCTYPE html>
<html>
  <head>
    <title>TA HW4</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- <link rel="stylesheet" href="/stylesheets/application.css"> -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  </head>

  <body>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Home</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse d-flex justify-content-end" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <% if @current_user %>
          <a class="nav-item py-1 pe-2">Logged in as: <%= @current_user.username %> </a>
          <a class="nav-item "><%= button_to "Logout", "/sessions/logout", method: :delete %></a>
        <% else %>      
          <a class="nav-link active" href="/sessions/new">Login</a>
          <a class="nav-link active" href="/users/new">Signup</a>
        <% end %>
      </div>
    </div>
  </div>
</nav>

    <div class="container">
<!--      <p>
        <% if @current_user %>
          Logged in as: <%= @current_user.username %>
          <%= button_to "Logout", "/sessions/logout", method: :delete %>
        <% else %>
          <a href="/">Home</a>
          ||
          <a href="/sessions/new">Login</a>
          ||
          <a href="/users/new">Signup</a>
        <% end %>
      </p>

      <p>
        <%= flash[:notice] %>
      </p>
-->
      <%= yield %>
      
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
  </body>
</html>
